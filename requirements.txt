requests>=2.31
beautifulsoup4>=4.12
brotli>=1.1
dnspython>=2.4             # DNS resolution for tools.py + email auth checks
lxml>=5.0                  # XML parsing for Steam profile etc.
phonenumbers>=8.13         # phone parsing/region/carrier (Email/Phone tab)
mmh3>=4.0                  # MurmurHash3 for favicon-hash Shodan pivots
imagehash>=4.3             # perceptual hashes for screenshot intel
Pillow>=10.0               # PIL fork — image decode for imagehash
playwright>=1.40           # optional: only needed when using --render
playwright-stealth>=2.0    # optional: anti-detection patches for --render
flask>=3.0                 # optional: only needed for webui.py
# install the local emailrep client editable: pip install -e ./emailrep.io-python
emailrep>=0.0.5            # sublime-security/emailrep.io-python — used by tools_email_phone.emailrep
